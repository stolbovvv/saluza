extends ./layouts/page.pug

include ./mixins/page-navbar.pug

block head
	-
		var title = 'Доставка и оплата - SALUZA';

block content
	-
		var heading = 'Доставка и оплата';
		var breadcrumbs = [
			{ text: 'Главная', href: '/' }
		];

	+page-navbar({ className: 'offset-header', heading: heading, links: breadcrumbs })

	main.main
