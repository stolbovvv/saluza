extends ./layouts/page.pug

include ./mixins/page-navbar.pug

block head
	-
		var title = 'Политика конфиденциальности - SALUZA';

block content
	-
		var heading = 'Политика конфиденциальности';
		var breadcrumbs = [
			{ text: 'Главная', href: '/' },
		];

	+page-navbar({ className: 'offset-header', heading: heading, links: breadcrumbs })

	main.main
