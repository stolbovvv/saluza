extends ./layouts/page.pug

include ./mixins/page-navbar.pug

block head
	-
		var title = 'Пользовательское соглашение - SALUZA';

block content
	-
		var heading = 'Пользовательское соглашение';
		var breadcrumbs = [
			{ text: 'Главная', href: '/' },
		];

	+page-navbar({ className: 'offset-header', heading: heading, links: breadcrumbs })

	main.main
